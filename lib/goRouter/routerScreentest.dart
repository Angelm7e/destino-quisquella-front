import 'package:flutter/material.dart';

class Routerscreentest extends StatelessWidget {
  const Routerscreentest({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
